#!/bin/bash

eval $(ssh-agent);

# Generated by arandr, it is a shell script containing an xrandr command.
~/.screenlayout/default.sh
setxkbmap -option compose:menu

google-chrome-beta --app=https://inbox.google.com/ --profile-directory="Profile 1"
google-chrome-beta --app=https://calendar.google.com/ --profile-directory="Profile 1"

(
parcellite &
sxhkd &
xcompmgr &
)
